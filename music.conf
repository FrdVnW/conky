conky.config = {

  background = true,
  use_xft = true,
  font = 'Liberation Mono:size=10',
  xftalpha = 1,

  own_window = true,
  own_window_type = 'normal',
  own_window_transparent = true,
  own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',
  own_window_argb_visual = true,

  double_buffer = true,
  draw_shades = false,
  draw_outline = false,
  draw_borders = false,

  stippled_borders = 0,
  border_width = 0,
  draw_graph_borders = true,

  default_color = '979797',
  color0 = '467f77',

  alignment = 'top_left',
  no_buffers = true,

  uppercase = false,

  cpu_avg_samples = 1,
  net_avg_samples = 1,
  override_utf8_locale = true,

  use_spacer = 'right',
  text_buffer_size = 2048,

  alignment = 'top_right',
  gap_x = 0,
  gap_y = 600,

  minimum_width = 320,
  minimum_height = 400,
  maximum_width = 320,

  update_interval = 10
}

conky.text = [[
${color0}Music  ${hr 2}
${color0}FIP playing now: ${color}${exec /bin/bash -c "fip --current-song"}
${color0}FIP playing soon: ${color}${exec /bin/bash -c "fip --next-song"}
]]
